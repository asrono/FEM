figure(2);
trisurf(elmat,x,y,u)
figure(3);
trisurf(elmat,x,y,u);
view(2); shading interp; colormap jet; colorbar; set(gcf,'renderer','zbuffer')

figure(4);

norm = /sqrt(v_x.^2+v_y.^2);
vx=v_x./sqrt(v_x.^2+v_y.^2);
vy=v_y./sqrt(v_x.^2+v_y.^2);
quiver(x,y,vx',vy',0.3); axis([-1 1 -1 1]); hold on
% contour(elmat,x,y,u);hold off;